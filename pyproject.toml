[project]
name = "africanlanguages"
version = "0.1.0"
description = "Utilities for African languages"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pycountry>=24.6.1",
]

[tool.uv]
package = true
